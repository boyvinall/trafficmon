version: "2"

formatters:
  enable:
    - gci

  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/boyvinall/mac-nethogs)

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
