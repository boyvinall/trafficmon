version: "2"

formatters:
  enable:
    - gci

  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/boyvinall/trafficmon)

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
